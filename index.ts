import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent ensures the app works correctly
// whether loaded via Expo Go or a standalone native build
registerRootComponent(App);
